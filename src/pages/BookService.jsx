import { Helmet } from "react-helmet-async";

const BookService = () => {
    return (
        <div>
            <Helmet>
                <title>Book service</title>
            </Helmet>
            <h2>Book service</h2>
        </div>
    );
};

export default BookService;